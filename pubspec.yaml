name: action
description: "A new Flutter project."

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.4.4 <4.0.0"

dependencies:
  core_y: ^0.3.1
  figma_squircle: ^0.5.3
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.5.1
  flutter_svg: ^2.0.10+1
  network_y: 0.1.1+2
  # notion_db_sdk: ^0.2.0
  notion_db_sdk:
    path: ../notion_db_sdk/
  wakelock_plus: ^1.2.8
  smart_textfield: ^1.0.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  mocktail: ^1.0.4
  lints_y:
    git:
      url: https://github.com/Yogi-7y/Project-Y
      path: packages/lints
      ref: main

flutter:
  uses-material-design: true
